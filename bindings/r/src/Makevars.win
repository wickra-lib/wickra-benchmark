# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_benchmark.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKBENCH_INC)
PKG_LIBS = -L$(WKBENCH_LIB) -l:wickra_benchmark.dll
